package com.mihir.alzheimerscaregiver.notifications;

// Client-only mode: No Firebase Messaging used.
// This placeholder avoids compile errors if the file still exists.
public class MessagingService {
    // Intentionally empty
}
